{{#routes}}

  <div class="route row" id="route-{{this.[0]}}">
    <span class="span1">Route {{this.[0]}}</span>

    <span class="span3">This is a placeholder</span>

    <span class="span8">
      <div class="scroller-wrapper">
        <div class="scroller">
          <ul class="times unstyled inline">
            {{#each this.[1]}}
              <li class="time" data-index="{{@index}}" data-timestamp="{{arrival_time}}" data-trip-headsign="{{trip_headsign}}">
                {{display_time arrival_time}}
              </li>
            {{/each}}
            <li>No more times scheduled for today.</li>
          </ul>
        </div>
      </div>
    </span>

  </div>

{{/routes}}
